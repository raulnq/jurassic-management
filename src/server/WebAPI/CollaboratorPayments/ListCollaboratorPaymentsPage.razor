@using WebAPI.Invoices;
@using WebAPI.Infrastructure.SqlKata;
@using WebAPI.Infrastructure.Ui;

<Heading Text=@Endpoints.Title />
<Breadcrumbs Links=@(new []{new Breadcrumbs.Link(Endpoints.ListTitle)})
             Target=@(Infrastructure.Ui.Targets.Workspace) />
<Toolbar>
    <Actions>
        <ActionButton Text=@Endpoints.RegisterTitle
                      Icon=@Icons.Add
                      HtmxProps=@(new HtmxProps(
                      Endpoints.Register, Infrastructure.Ui.Targets.Workspace)) />
        <ActionButton Text=@ProformaToCollaboratorPaymentProcesses.Endpoints.RegisterTitle
                      Icon=@Icons.Add
                      HtmxProps=@(new HtmxProps(
                      ProformaToCollaboratorPaymentProcesses.Endpoints.Register, Infrastructure.Ui.Targets.Workspace)) />
    </Actions>
</Toolbar>
<Section>
    <Content>
        <FormRow>
            <Content>
                <div class="col">
                    <SelectFilter HtmxProps=@(new HtmxProps(Endpoints.List, $"#{Targets.List}",Swap.OuterHTML,$"#{Targets.List}"))
                                  Label="Status"
                                  Property=@(nameof(ListCollaboratorPayments.Query.Status))
                                  Query=@Query
                                  EmptyLabel="All Status"
                                  Source=@(Enum.GetValues<CollaboratorPaymentStatus>().ToDictionary(i=>i.ToString(), i=>i.ToString())) />
                </div>
            </Content>
        </FormRow>
        <div id=@Targets.List>
            <DataTable TResult=ListCollaboratorPayments.Result
                       Result=@Result
                       Context="item">
                <TableHeader>
                    <th></th>
                    <th>CollaboratorPaymentId</th>
                    <th>Collaborator</th>
                    <th>Gross Salary</th>
                    <th>Withholding</th>
                    <th>Net Salary</th>
                    <th>Status</th>
                    <th>Number</th>
                </TableHeader>
                <RowTemplate>
                    <RowToolbar>
                        <Actions>
                            <ActionLink HtmxProps=@(new HtmxProps(Endpoints.GetEdit(@item.CollaboratorPaymentId), Infrastructure.Ui.Targets.Workspace))
                                        Icon=@Icons.Edit />
                        </Actions>
                    </RowToolbar>
                    <td>@item.CollaboratorPaymentId</td>
                    <td>@item.CollaboratorName</td>
                    <td>@item.Currency @item.GrossSalary</td>
                    <td>@item.Currency @item.Withholding</td>
                    <td>@item.Currency @item.NetSalary</td>
                    <td>@item.Status</td>
                    <td>@item.Number</td>
                </RowTemplate>
            </DataTable>
            <Pagination Query=@Query
                        Result=@Result
                        HtmxProps=@(new HtmxProps(Endpoints.List, $"#{Targets.List}", Swap.OuterHTML, $"#{Targets.List}"))
                        Parameters=@(new []{ new KeyValuePair<string, object?>(nameof(ListCollaboratorPayments.Query.Status), Query.Status)}) />
        </div>
    </Content>
</Section>
@code {
    [Parameter, EditorRequired]
    public ListResults<ListCollaboratorPayments.Result> Result { get; set; } = default!;
    [Parameter, EditorRequired]
    public ListCollaboratorPayments.Query Query { get; set; } = default!;
}
