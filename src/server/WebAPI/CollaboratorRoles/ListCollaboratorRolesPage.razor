@using WebAPI.CollaboratorRoles;
@using WebAPI.Infrastructure.SqlKata;
@using WebAPI.Infrastructure.Ui;

<Heading Text=@Endpoints.Title />
<Breadcrumbs Links=@(new []{new Breadcrumbs.Link(Endpoints.ListTitle)})
             Target=@(Infrastructure.Ui.Targets.Workspace) />
<Toolbar>
    <Actions>
        <ActionButton Text=@Endpoints.RegisterTitle
                      Icon=@Icons.Add
                      HtmxProps=@(new HtmxProps(Endpoints.Register, Infrastructure.Ui.Targets.Workspace)) />
    </Actions>
</Toolbar>
<Section>
    <Content>
        <FormRow>
            <Content>
                <div class="col">
                    <SearchFilter Label="Name"
                                  Property=@nameof(ListCollaboratorRoles.Query.Name)
                                  Query=@Query
                                  HtmxProps=@(new HtmxProps(Endpoints.List, $"#{Targets.List}", Swap.OuterHTML, $"#{Targets.List}")) />
                </div>
            </Content>
        </FormRow>
        <div id=@Targets.List>
            <DataTable TResult=ListCollaboratorRoles.Result
                       Result=@Result
                       Context="item">
                <TableHeader>
                    <th></th>
                    <th>CollaboratorRoleId</th>
                    <th>Name</th>
                    <th>Fee Amount</th>
                    <th>Profit Percentage</th>
                </TableHeader>
                <RowTemplate>
                    <RowToolbar>
                        <Actions>
                            <ActionLink HtmxProps=@(new HtmxProps(Endpoints.GetEdit(@item.CollaboratorRoleId), Infrastructure.Ui.Targets.Workspace))
                                        Icon=@Icons.Edit />
                        </Actions>
                    </RowToolbar>
                    <td>@item.CollaboratorRoleId</td>
                    <td>@item.Name</td>
                    <td>$ @item.FeeAmount</td>
                    <td>@item.ProfitPercentage %</td>
                </RowTemplate>
            </DataTable>
            <Pagination Query=@Query
                        Result=@Result
                        HtmxProps=@(new HtmxProps(Endpoints.List, $"#{Targets.List}", Swap.OuterHTML, $"#{Targets.List}"))
                        Parameters=@(new[] { new KeyValuePair<string, object?>(nameof(Query.Name), Query.Name) }) />
        </div>
    </Content>
</Section>
@code {
    [Parameter, EditorRequired]
    public ListResults<ListCollaboratorRoles.Result> Result { get; set; } = default!;
    [Parameter, EditorRequired]
    public ListCollaboratorRoles.Query Query { get; set; } = default!;
}
